## Packages
react-hook-form | Form handling for dynamic forms
@hookform/resolvers | Zod integration for forms
zod | Schema validation
framer-motion | Animations for transitions and interactions
lucide-react | Icons
clsx | Class name utility
tailwind-merge | Class name utility
date-fns | Date formatting

## Notes
Auth is handled via Replit Auth (useAuth hook already exists).
Dynamic forms need to parse JSONB config from backend.
Using 'Inter' and 'Space Grotesk' for typography.
